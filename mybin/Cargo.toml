# https://doc.rust-lang.org/cargo/reference/manifest.html

[package]
name = "mybin"
version = "0.1.0"
edition = "2021"

[dependencies]
mylib = { path = "../mylib", version = "*" }
rand = "0.8.*"
futures = "0.3.*"

[dev-dependencies]
